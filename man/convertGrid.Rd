% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featSpeech.R
\name{convertGrid}
\alias{convertGrid}
\title{Convert TextGrid Outputs into Dataframes}
\usage{
convertGrid(
  ls.files,
  rs.path = c(),
  suffix = "",
  prefix = "",
  extract = T,
  verbose = T,
  recompute = F,
  return = F
)
}
\arguments{
\item{ls.files}{Character vector. Paths for the TextGrid files, including filename and extension.}

\item{rs.path}{Character. Path to the directory where the output files will be saved.
If empty (\code{is.null(rs.path) == TRUE}), nothing is saved to disk. Default is \code{c()}.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{prefix}{Character. Prefix for the filename allowing extraction of Dyad and Identifier from filenames. Default is \code{""}.}

\item{extract}{Logical. Whether Dyad and Identifier variables are extracted from the file names.
Requires the filename structure: \code{"[prefix][Dyad]_[Identifier]_*"}. Default is \code{TRUE}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
If \code{return = TRUE}, returns the processed dataframe. Saves \verb{dataUhm[suffix].arrow} to disk if \code{rs.path} is provided.
}
\description{
This function converts the TextGrid output produced by the uhm-o-meter into a
dataframe containing all sounding instances and the total number of syllables.
}
\references{
de Jong & Wempe (2009). Behavior Research Methods.

de Jong, Pacilly & Heeren (2021). Assessment in Education: Principles, Policy and Practice.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
