% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featSpeech.R
\name{featSpeech}
\alias{featSpeech}
\title{Aggregate Acoustic Information Extracted via Praat}
\usage{
featSpeech(
  df.speak,
  praat.path,
  praat.prefix,
  rs.path,
  suffix = "",
  verbose = T,
  recompute = F,
  return = F
)
}
\arguments{
\item{df.speak}{Dataframe containing all information about the sounding instances,
typically created using \code{\link[=convertGrid]{convertGrid()}}.}

\item{praat.path}{Character. Path to the directory containing the Praat output files.}

\item{praat.prefix}{Character. Prefix used in the Praat script for the output files.}

\item{rs.path}{Character. Path to the directory where the output files will be saved.
If empty (\code{is_empty(rs.path) == TRUE}), nothing is saved to disk.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{FALSE}.}
}
\value{
If \code{return = TRUE}, returns the processed dataframe. Saves \verb{featSpeech[suffix].csv} to disk if \code{rs.path} is provided.
}
\description{
This function aggregates all of the information extracted from \code{featSpeech.praat},
which includes the aggregated scores from the uhm-o-meter (de Jong et al., 2021).
}
\references{
de Jong & Wempe (2009). Behavior Research Methods.

de Jong, Pacilly & Heeren (2021). Assessment in Education: Principles, Policy and Practice.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
