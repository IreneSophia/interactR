% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featDwell.R
\name{iterSmoothing}
\alias{iterSmoothing}
\title{Iteratively Smooth Categorical Data}
\usage{
iterSmoothing(x, niter = 4)
}
\arguments{
\item{x}{Character. A character or factor vector ordered by frame or time, i.e. AOI classifications}

\item{niter}{Numeric. An integer specifying the maximum number of smoothing passes to perform
(default is \code{10}). The loop terminates early if no further changes are detected.}
}
\value{
A character or factor vector of the same length as \code{x}, with isolated
one or two-frame discrepancies smoothed out.
}
\description{
Removes single- and two-frame transient noise or "stranglers" from a categorical
vector (such as eye-tracking AOIs) by iteratively replacing isolated values
that are sandwiched between identical preceding and succeeding values.
}
\details{
The function operates in an iterative loop, performing two sequential checks per pass:
\enumerate{
\item \strong{Single-frame gaps (width 3):} Evaluates overlapping three-frame windows. If the left and right
elements match each other (\code{left == right}) but differ from the middle element (\code{mid != left}),
the middle element is overwritten with the outer value.
\item \strong{Two-frame gaps (width 6):} Evaluates six-frame windows to check if a block of two identical
values (\code{m1 == m2}) is encased by matching outer blocks of two frames each (\code{l1 == l2}, \code{r1 == r2},
and \code{l1 == r1}), where the middle differs from the outer (\code{m1 != l1}). If matched, both middle
frames are overwritten with the outer value.
}

Single-frame gaps are cleared first, followed immediately by two-frame gaps, ensuring cascading
artifacts are fully resolved across multiple iterations until the vector stabilises.
}
\examples{
\dontrun{

# Example sequence with isolated "Desk" glitches inside "Head" looks
AOI = c("Head", "Head", "Desk", "Head", "Desk", "Head", "Head")

iterSmoothing(AOI)
# Returns: c("Head" "Head" "Head" "Head" "Head" "Head" "Head")

}

}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
