% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featWavelet.R
\name{extractWTC}
\alias{extractWTC}
\title{Compute Wavelet Coherence for Time-course Data}
\usage{
extractWTC(
  df,
  rs.path,
  colname,
  fps,
  order = 8,
  suffix = "",
  nsim = 400,
  pseudoDyad = F,
  nDyads = NULL,
  shuffleMethod = "Identifier",
  seed = "random",
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe. The dataset containing the variables to be processed.
Must explicitly feature columns \code{Dyad}, \code{Identifier}, \code{Time}, either \code{Frame} or \code{Timestamp}, and the column \code{colname}.
For each Dyad, there must be exactly two Identifiers in the data.}

\item{rs.path}{Character. Path to destination directory for saved files. If empty (is.null(rs.path) == TRUE), then nothing is saved.}

\item{colname}{Character. The exact name in \code{df} from which to extract the data.}

\item{fps}{Numeric. Frame processing rate frequency profile (frames per second) of the dataset.}

\item{order}{Numeric. Order for the wavelet transformation. Default is \code{8} based on Issartel et al. (2006).}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{pseudoDyad}{Logical. Flags whether to generate pseudo-WTC benchmarks using dyad shuffling
instead of observed WTC Default is \code{FALSE}.}

\item{nDyads}{Numeric. Total number of synthetic dyad simulations to execute when \code{pseudoDyad = TRUE}.
Default is \code{NULL} which is converted into the number of real dyads.}

\item{shuffleMethod}{Character. Either \code{"Identifier"} or \code{"Dyad"}. If the first, then
the function \code{\link[=shuffleIdentifier]{shuffleIdentifier()}} is used, otherwise \code{\link[=shuffleDyads]{shuffleDyads()}}.}

\item{seed}{Character or Numeric. Seed supporting reproducibility. Takes an integer seed or \code{"random"}. Default is \code{"random"}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}

\item{nsim.}{Numeric. Number of Monte Carlo randomisations for computing WTC. Default is \code{400}.}
}
\value{
If \code{return = TRUE}, returns list with the results. If provided, the full list is saved as rds to \code{rs.path}.
}
\description{
Uses the \code{\link{biwavelet::wtc}} function to compute the Wavelet Coherence
and Phase for a given column for a dyad. Optionally creates pseudo coherence based
on Dyad shuffling. Resulting list can be transformed to dataframe using \code{\link[=convertWTC]{convertWTC()}}.
Observed and pseudo coherence can be compared using \code{\link[=featWTC]{featWTC()}}.
}
\references{
Issartel et al. (2006): A Practical Guide to Time—Frequency Analysis
in the Study of Human Motor Behavior: The Contribution of Wavelet Transform,
Journal of Motor Behavior, 38(2), 139-159.
}
\seealso{
\code{\link{convertWTC}} \code{\link{featWTC}}
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
