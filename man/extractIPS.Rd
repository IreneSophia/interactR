% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featIPS.R
\name{extractIPS}
\alias{extractIPS}
\title{Extract Observed or Pseudo Interpersonal Synchrony (IPS)}
\usage{
extractIPS(
  df,
  colname,
  method,
  fps,
  featname = NA,
  rs.path = c(),
  suffix = "",
  cores = NA,
  winSample = NA,
  incSample = NA,
  lagSample = NA,
  order = 8,
  nSegment = 0,
  df.pseudo = data.frame(),
  seed = NA,
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe. Must contain the columns \code{Dyad}, \code{Time}, \code{Identifier},
\code{Frame} as well as the column described by \code{colname}.}

\item{colname}{Character. Name of the column from which IPS is extracted.}

\item{method}{Character. Name of the method to compute IPS: either "WTC" for
wavelet coherence or "WLCC" for windowed lagged cross correlation.}

\item{fps}{Numeric. Frame processing rate frequency profile (frames per second) of the dataset.}

\item{featname}{Character. Name of the Feature. If \code{NA}, then the colname is used. Default is \code{NA}.}

\item{rs.path}{Character. Path to destination directory for saved files.
If empty (is.null(rs.path) == TRUE), then nothing is saved. Default is \code{c()}.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{cores}{Numeric. How many cores to use for parallelisation.
Default is \code{NA}, translating to as many as possible.}

\item{winSample}{Numeric. Window size in samples for WLCC. Default is \code{NA}.}

\item{incSample}{Numeric. Window increment step in samples for WLCC. Default is \code{NA}.}

\item{lagSample}{Numeric. Evaluated cross-correlation lag step in samples for WLCC. Default is \code{NA}.}

\item{order}{Numeric. Order for the wavelet transformation. Default is \code{8} based on Issartel et al. (2006).}

\item{nSegment}{Numeric. If larger than 0, then instead of extracting observed IPS,
windows are shuffled to created pseudo IPS. Number of shuffling to be performed.
Default is \code{100}.}

\item{df.pseudo}{Dataframe. If it contains rows, then instead of using the observed
dyads listed in \code{df}, these pairings are tested for pseudo IPS. Dataframe can be
created using \code{\link[=shuffleIdentifier]{shuffleIdentifier()}} or \code{\link[=shuffleDyads]{shuffleDyads()}}. Default is \code{data.frame()}.}

\item{seed}{Numeric. Seed supporting reproducibility. Takes an integer seed or \code{NA} for random. Default is \code{NA}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
If return is TRUE, then IPS dataframe. If rs.path is provided, then the dataframe is also saved there.
}
\description{
This function computes IPS either using Wavelect Coherence (WTC, \code{\link[=computeWTC]{computeWTC()}}) or using
Windowed-Lagged Cross-Correlation (WLCC, \code{\link[=computeWLCC]{computeWLCC()}}). It allows for a \code{df.pseudo}
to be provided to compute pseudo IPS based on dyad shuffling as well. Furthermore,
one can compute pseudo IPS based on segment shuffling by setting nSegment > 0.
}
\details{
If pseudo IPS is computed, this should be either through setting pseudoSegment to TRUE
or by using a df.pseudo to provide shuffled dyads, but not both.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
