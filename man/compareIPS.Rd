% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featIPS.R
\name{compareIPS}
\alias{compareIPS}
\title{Compares pseudo and observed IPS for each Lag (WLCC) or each Frequency (WTC)}
\usage{
compareIPS(
  df,
  Bayesian = T,
  perm = F,
  minBF = log(3),
  alpha = 0.05,
  freqLimits = c(0.2, 8),
  rs.path = c(),
  suffix = "",
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe. Dataframe created by \code{\link[=extractIPS]{extractIPS()}} extracting pseudo and observed values.
Must contain the columns \code{Dyad}, \code{Method}, \code{Feature} and either \code{Lag} and \code{WLCC}
or \code{Frequency} and \code{Rsq}. If permutation is to be performed, must also contain the
column \code{iteration}.}

\item{Bayesian}{Logical. Switch to perform Bayesian or frequentist testing. Default is \code{TRUE}.}

\item{perm}{Logical. Switch to use permutation testing instead of comparison across Dyads.
If the dataset is smaller, then one can create a larger set of pseudo values
against which the mean observed value per Lag can be compared. Default is \code{FALSE}.}

\item{minBF}{Numeric. Threshold above which log Bayes Factor is considered credible evidence. Default is \code{log(3)}.}

\item{alpha}{Numeric. Threshold above which permutation and Frequentist is considered significant. Default is \code{0.05}.}

\item{rs.path}{Character. Path to destination directory for saved files.
If empty (is.null(rs.path) == TRUE), then nothing is saved. Default is \code{c()}.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
If \code{return = TRUE}, returns dataframe. If provided, the dataframe is saved as rds to \code{rs.path}.
}
\description{
Takes the dataframe created by \code{\link[=extractIPS]{extractIPS()}} and compares the pseudo and the
observed IPS values based on the mean. Comparison can be either made by
aggregating the values across Dyads and then comparing the pseudo and the
observed z-transformed values for each lag or Frequency with a t-test to assess.
Frequentist or Bayesian t-tests can be chosen with the \code{Bayesian} logical switch.
Alternatively, if the sample of dyads is small, observed IPS can be aggregated
across all dyads and compared with a larger sample of pseudo IPS in a
permutation test by setting \code{perm = TRUE}. When frequentist t-tests are computed,
multiple comparison correction is applied based on Benjamini & Hochberg's (1995) FDR.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
