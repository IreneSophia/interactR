% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featSpeech.R
\name{addCommunication}
\alias{addCommunication}
\title{Add Conversational States Based on Praat Output}
\usage{
addCommunication(
  df,
  df.speak,
  rs.path = c(),
  suffix = "",
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe containing the tracked data. Must contain the columns \code{Dyad},
\code{Time}, \code{Frame} and \code{Timestamp} (in POSIX format).}

\item{df.speak}{Dataframe containing all information about the sounding instances,
typically created using \code{\link[=convertGrid]{convertGrid()}}.
Must contain the columns \code{Dyad}, \code{Identifier}, \code{Start}, and \code{End} (both in seconds).}

\item{rs.path}{Character. Path to the directory where the output files will be saved.
If empty (\code{is.null(rs.path) == TRUE}), nothing is saved to disk. Default is \code{c()}.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
If \code{return = TRUE}, returns the processed dataframe. Saves \verb{data[suffix].arrow} to disk if \code{rs.path} is provided.
}
\description{
This function adds the \code{Listening}, \code{Speaking}, and \code{Communication} state columns
based on speech analysis performed in Praat using the uhm-o-meter developed by
de Jong et al. (2021). If these columns already exist in the dataset, they will
be dynamically renamed with the suffix \code{"_Original"}.
}
\references{
de Jong & Wempe (2009). Behavior Research Methods.

de Jong, Pacilly & Heeren (2021). Assessment in Education: Principles, Policy and Practice.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
