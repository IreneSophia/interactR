% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featSpeech.R
\name{rewriteGrid}
\alias{rewriteGrid}
\title{Filter and Rewrite TextGrids for Manual Auditory Inspection}
\usage{
rewriteGrid(
  ls.files,
  rs.path,
  minSyll = 1,
  maxSyll = 4,
  max.nos = Inf,
  min.dist = 0
)
}
\arguments{
\item{ls.files}{Character vector. Full system paths and filenames pointing to target TextGrid files.}

\item{minSyll}{Numeric. Minimum syllables a valid sounding instance must contain. Default is \code{1}.}

\item{maxSyll}{Numeric. Maximum syllables a valid sounding instance can contain. Default is \code{4}.}

\item{max.nos}{Numeric. Maximum total number of sounding instances to keep. Default is \code{Inf}.}

\item{min.dist}{Numeric. Minimum distance in seconds required between separate sounding instances. Default is \code{0}.}
}
\value{
Saves reconstructed TextGrid files to disk in the same folder as original file named as \verb{[filename]_check-[nos].TextGrid}.
}
\description{
The output of the uhm-o-meter requires visual and manual auditory inspection.
Most misclassifications affect sounding instances with few syllables (\code{nSyll}),
capturing loud breathing rather than authentic speaking. This utility function exports
targeted TextGrids containing only specific syllable-count ranges to ease down-stream validation.
}
\references{
de Jong & Wempe (2009). Behavior Research Methods.

de Jong, Pacilly & Heeren (2021). Assessment in Education: Principles, Policy and Practice.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
