% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featWLCC.R
\name{featWLCC}
\alias{featWLCC}
\title{Compute Windowed-Lagged Cross-Correlations (WLCC), potentially with Pseudo-Synchrony Comparison}
\usage{
featWLCC(
  df,
  rs.path,
  colname,
  featname,
  win,
  inc,
  lag,
  fps,
  suffix = "",
  parallel = T,
  pseudoDyad = T,
  nDyad = 100,
  bfThreshold = log(3),
  pseudoShuffling = F,
  shuffleMethod = "Seg",
  nShuffle = 100,
  pseudoPass = 1,
  credibleThreshold = 90,
  method = "peak",
  r2Z = T,
  ABS = T,
  seed = "random",
  plot = T,
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe containing tracking data. Requires the variables \code{Dyad}, \code{Identifier}, \code{Frame} and the target numeric timeline (\code{colname}).}

\item{rs.path}{Character. Path to destination directory. Files are written inside a generated directory nested under \verb{[rs.path]/featWLCC[suffix]}.
If empty (is.null(rs.path) == TRUE), then nothing is saved.}

\item{colname}{Character. Name of column vector inside \code{df} to isolate for analysis.}

\item{featname}{Character. Descriptive label for feature. Must not contain underscores.}

\item{win}{Numeric. Window size in seconds.}

\item{inc}{Numeric. Window increment step in seconds.}

\item{lag}{Numeric. Evaluated cross-correlation lag step in seconds.}

\item{fps}{Numeric. Sampling rate (frames per second).}

\item{suffix}{Character. Suffix string appended onto \code{rs.path}. Default is \code{""}.}

\item{parallel}{Logical. Enables multi-core clusters inside the parent \code{MEAccf} routine. Default is \code{TRUE}.}

\item{pseudoDyad}{Logical. Flags whether to generate pseudo-WLCC benchmarks using dyad shuffling. Default is \code{TRUE}.}

\item{nDyad}{Numeric. Total number of synthetic dyad simulations to execute when \code{pseudoDyad = TRUE}. Default is \code{100}.}

\item{bfThreshold}{Numeric. Credibility threshold for evaluation of log-transformed Bayes Factors. Default is \code{log(3)}.}

\item{pseudoShuffling}{Logical. Flags whether to generate pseudo-WLCC benchmarks using within-series random shuffles. Default is \code{FALSE}.}

\item{shuffleMethod}{Character. Assigns segment shuffling (\code{"Seg"}) or cell shuffling (\code{"Data"}). Default is \code{"Seg"}.}

\item{nShuffle}{Numeric. Shuffling iterations for \code{pseudoShuffling} per dyad. Default is \code{100}.}

\item{pseudoPass}{Numeric. Passing threshold to determine credible lags based on different pseudo-WLCC methods. Default is \code{1}.}

\item{credibleThreshold}{Numeric. Percentile cutoff for credibility based on permutations. Default is \code{90}.}

\item{method}{Character. Perform either peak extraction (\code{"peak"}) or window averages (\code{"mean"}) for observed WLCC. Default is \code{"peak"}.}

\item{r2Z}{Logical. Applies Fisher's Z transformation over raw cross-correlation indexes. Default is \code{TRUE}.}

\item{ABS}{Logical. Maps vectors using absolute values, filtering out directionality of WlCC. Default is \code{TRUE}.}

\item{seed}{Universal flag supporting reproducibility. Takes an integer seed or \code{"random"}. Default is \code{"random"}.}

\item{plot}{Logical. Generates PDF evaluation outputs in the output folder. Default is \code{TRUE}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
Summary dataframe arrays when \code{return = TRUE}. Creates a structured directory bundle featuring
\verb{_ccf.rds}, \verb{_df-agg.csv}, \verb{_df.csv} datasets for observed and pseudo-WLCC,
statistical tables (\verb{_pseudo-comp.csv}), and structural visualization plots (\code{.pdf}). This bundle
is only created if \code{!is.null(rs.path)}.
}
\description{
Computes empirical and pseudo windowed-lagged cross-correlations using backend tools from \code{rMEA}.
}
\note{
Requires package \code{rMEA} (>= 1.3.1) to support generating diagnostic heatmap distributions when \code{ABS = FALSE}.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
