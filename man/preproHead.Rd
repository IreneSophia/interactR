% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featHead.R
\name{preproHead}
\alias{preproHead}
\title{Preprocess Head Motion Coordinates}
\usage{
preproHead(
  df,
  rs.path,
  rotnames,
  tranames,
  suffix = "",
  performFixCirc = T,
  cornames = c(),
  verbose = T,
  recompute = F,
  return = F
)
}
\arguments{
\item{df}{Dataframe containing head coordinate streams. Requires tracking coordinates alongside core columns \code{Dyad}, \code{Identifier} and \code{Frame}.}

\item{rs.path}{Character. Path to the directory where the output files will be saved.
If empty (\code{is_empty(rs.path) == TRUE}), nothing is saved to disk.}

\item{rotnames}{Character vector. String labels identifying target rotational columns.}

\item{tranames}{Character vector. String labels identifying target translational columns.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{performFixCirc}{Logical. Directs execution to correct boundaries for angular circular values. Default is \code{TRUE}.}

\item{cornames}{Character vector. Optional mapping labels matched in length to \code{rotnames} for baseline adjustments
(e.g., using spine tracking vectors to separate head posture from whole-body sway). Default is \code{c()}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{FALSE}.}
}
\value{
If \code{return = TRUE}, returns an adjusted movement dataframe. Saves \verb{dataHead[suffix].rds} to \code{rs.path} if provided.
}
\description{
Preprocesses spatial head trajectory streams. Rotational paths are scrubbed of wrap-around geometric circularity
or baseline-adjusted using corresponding body segments. Circularity corrected and translational trajectories are detrended.
}
\references{
Hale et al. (2020). Journal of Nonverbal Behavior.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
