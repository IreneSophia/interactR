% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featWLCC.R
\name{pseudoWLCC}
\alias{pseudoWLCC}
\title{Permutation-Based Pseudosynchrony for Windowed-Lagged Cross-Correlation}
\usage{
pseudoWLCC(
  shuffleMethod,
  mea.orig,
  fl.wlcc,
  rs.path = c(),
  win = win,
  inc = inc,
  lag = lag,
  fps = fps,
  parallel = T,
  n = 100,
  r2Z = T,
  ABS = T,
  seed = "random",
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{shuffleMethod}{Character string. Specifies the shuffling paradigm; accept options are either \code{"Data"} or \code{"Seg"}.}

\item{mea.orig}{List containing \code{MEA} objects (can be created via \code{\link[=MEAfake]{MEAfake()}}).}

\item{fl.wlcc}{Character. Prefix for files saved to disk.}

\item{rs.path}{Character. Path to destination directory. Files are written inside a generated directory nested under \verb{[rs.path]/featWLCC[suffix]}.
If empty (is.null(rs.path) == TRUE), then nothing is saved. Default is \code{c()}.}

\item{win}{Numeric. Window size in seconds.}

\item{inc}{Numeric. Window increment step in seconds.}

\item{lag}{Numeric. Evaluated cross-correlation lag step in seconds.}

\item{fps}{Numeric. Sampling rate (frames per second).}

\item{parallel}{Logical. Enables multi-core clusters inside the parent \code{MEAccf} routine. Default is \code{TRUE}.}

\item{n}{Numeric. Total quantity of individual permutations for each \code{MEA} object. Default is \code{100}.}

\item{r2Z}{Logical. Applies Fisher's Z transformation over raw cross-correlation indexes. Default is \code{TRUE}.}

\item{ABS}{Logical. Maps vectors using absolute values, filtering out directionality Default is \code{TRUE}.}

\item{seed}{Universal flag supporting reproducibility. Takes an integer seed or \code{"random"}. Default is \code{"random"}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
If \code{return = TRUE}, returns aggregated pseudo-WLCC values. Automatically saves \code{ls.psync} to disk if \code{rs.path} is provided.
}
\description{
Evaluates baseline pseudosynchrony indicators leveraging \code{rMEA::CCF}.
Based on methods documented in Moulder et al. (2018).
}
\details{
\itemize{
\item Assigning \code{"Data"} tests time independence: shuffles all data points in one of the two sequences.
\item Assigning \code{"Seg"} tests regional independence: slices one sequence into windows of length \code{win} and shuffles these segments.
}
}
\references{
Moulder, R. G., et al. (2018). Psychol Methods.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
