% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featIPS.R
\name{computeWTC}
\alias{computeWTC}
\title{Compute Wavelet Coherence for Time-course Data}
\usage{
computeWTC(
  df,
  fps,
  order = 8,
  rs.path = c(),
  suffix = "",
  seed = NA,
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe. The dataset containing the variables to be processed.
Must explicitly feature columns \code{Dyad}, \code{Frame}, \code{left} and \code{right}.
For each Dyad, there must be exactly two Identifiers in the data.}

\item{fps}{Numeric. Frame processing rate frequency profile (frames per second) of the dataset.}

\item{order}{Numeric. Order for the wavelet transformation. Default is \code{8} based on Issartel et al. (2006).}

\item{rs.path}{Character. Path to destination directory for saved files.
If empty (is.null(rs.path) == TRUE), then nothing is saved. Default is \code{c()}.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{seed}{Numeric. Seed supporting reproducibility. Takes an integer seed or \code{NA} for random. Default is \code{NA}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
If \code{return = TRUE}, returns dataframe with the results. If provided, the wtc object is saved as rds to \code{rs.path}.
}
\description{
Uses the \code{\link{biwavelet::wtc}} function to compute the Wavelet Coherence
and Phase for one dyad.
}
\details{
Relative phase differences are calculated as Phi_left - Phi_right. Thus, if Phase values
are positive, this indicates the left Identifier was leading, while negative values indicate that
the right Identifier was leading (based on the Dyad ID). Values closer to 0 indicate in-phase
and values closer to +- pi indicate anti-phase. No Monte Carlo randomisations are computed.
}
\references{
Issartel et al. (2006): A Practical Guide to Time—Frequency Analysis
in the Study of Human Motor Behavior: The Contribution of Wavelet Transform,
Journal of Motor Behavior, 38(2), 139-159.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
