% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featWavelet.R
\name{featWTC}
\alias{featWTC}
\title{Aggregate a dataframe containing the results of WTC to extract relevant features}
\usage{
featWTC(
  df,
  rs.path,
  phaseLimits,
  rsqLimits,
  FUN,
  withinCOI = T,
  onlySig = T,
  suffix = "",
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe. Dataframe created by \code{\link[=convertWTC]{convertWTC()}}.}

\item{rs.path}{Character. Path to destination directory for saved files. If empty (is.null(rs.path) == TRUE), then nothing is saved.}

\item{phaseLimits}{Numeric. Frequency limits in Hz for the Phase Bins. All but the last value specify the included lower limit.
The last value is the excluded maximum period. Must include at least two values.}

\item{rsqLimits}{Numeric. Frequency limits in Hz for the Coherence Bins. All but the last value specify the included lower limit.
The last value is the excluded maximum period. Must include at least two values.}

\item{withinCOI}{Logical. Whether only values inside the COI should be included. Defalut is \code{TRUE}.}

\item{onlySig}{Logical. Whether only significant values should be included. Defalut is \code{TRUE}.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}

\item{FUN.}{Function. Function to be used for the aggregation, e.g., mean or max (for peak).}
}
\value{
If \code{return = TRUE}, returns dataframe. If provided, the dataframe is saved as rds to \code{rs.path}.
}
\description{
Takes the dataframe created by \code{\link[=convertWTC]{convertWTC()}} and aggregates the results in prespecified Bins
and across all Bins within the specified limits. Limits may differ between Rsq and Phase.
Values outside COI and not significant values can be excluded.
}
\details{
Relative phase difference was calculated as Phi_left - Phi_right. Thus, if Phase values
are positive, this indicates the left Identifier was leading, while negative values indicate that
the right Identifier was leading (based on the Dyad ID). Values closer to 0 indicate in-phase
and values closer to +- pi indicate anti-phase.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
