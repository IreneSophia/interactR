% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featWavelet.R
\name{featWTC}
\alias{featWTC}
\title{Aggregate a dataframe containing the results of WTC to extract relevant features}
\usage{
featWTC(
  df,
  Limits,
  Funs,
  Bins = 8,
  rs.path = c(),
  suffix = "",
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe. Dataframe created by \code{\link[=extractIPS]{extractIPS()}}.}

\item{Limits}{List. List containing one or two dataframes with numeric of frequency limits in Hz.
First entry is used for the Coherence Bins, optionally a second for Phase Bins. If only
one dataframe with columns \code{upper} and \code{lower} as limits is provided, it is used for both.
Lower limits are included in the Bins but upper limits are not.}

\item{Funs}{List. List of function to be used for the aggregation. First is used for Coherence.
Optionally, a second is used for Phase. If Funs contains only one function,
then this function is used for both Phase and Coherence.}

\item{Bins}{Numeric. Number of Angle Bins for Phases. Default is \code{8}.}

\item{rs.path}{Character. Path to destination directory for saved files.
If empty (is.null(rs.path) == TRUE), then nothing is saved. Default is \code{c()}.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
If \code{return = TRUE}, returns dataframe. If provided, the dataframe is saved as a csv to \code{rs.path}.
}
\description{
Takes the dataframe created by \code{\link[=extractIPS]{extractIPS()}} and aggregates the results in prespecified Frequency
Bins and for Phase also in percentages of Angle Bins. Limits for Bins and aggregation function
may differ between Rsq and Phase. Values outside COI are excluded.
}
\details{
Relative phase difference was calculated as Phi_left - Phi_right. Thus, if Phase values
are positive, this indicates the left Identifier was leading, while negative values indicate that
the right Identifier was leading (based on the Dyad ID). Values closer to 0 indicate in-phase
and values closer to +- pi indicate anti-phase.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
