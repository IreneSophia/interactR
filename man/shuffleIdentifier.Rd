% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{shuffleIdentifier}
\alias{shuffleIdentifier}
\title{Shuffle one identifier of a dyad to create pseudo-dyads}
\usage{
shuffleIdentifier(df, seed = NULL, side = "right", nsim = 100)
}
\arguments{
\item{df}{Dataframe. Must contain the columns \code{Dyad}, \code{Time} and \code{Identifier}
to unequivocally identify one session. Each Dyad has one row per Identifier.
Dyad ID must consist of \verb{[Identifier]-[Identifier]}.}

\item{seed}{Numeric or other. Seed for reproducibility. If seed is not
numeric, then a random seed is chosen. Default is \code{NULL}.}

\item{side}{Character. Which side to shuffle. Default is \code{right}.}

\item{nsim}{Numeric. Number of pseudo-dyads to be returned. Default is \code{100}.}
}
\value{
Returns a dataframe with pseudo-dyads, including the information of
\code{Dyad}, now consisting of both original dyad IDs, as well as each two
columns for the left and the right \code{Identifer} and \code{Time}, thus, one row
per pseudo-dyad.
}
\description{
Takes a dataframe of dyads and returns a specific number of pseudo-dyads of
interaction partners who have not interacted with each other by shuffling  either
the left or the right Identifier.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
