% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featWavelet.R
\name{compWT}
\alias{compWT}
\title{Compute Wavelet Transformation for Time-course Data}
\usage{
compWT(
  df,
  rs.path,
  colname,
  fps,
  order = 6,
  suffix = "",
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe. The dataset containing the variables to be processed.
Must explicitly feature columns \code{Dyad}, \code{Identifier}, \code{Time}, either \code{Frame} or \code{Timestamp} and the column \code{colname}.}

\item{rs.path}{Character. Path to destination directory for saved files. If empty (is.null(rs.path) == TRUE), then nothing is saved.}

\item{colname}{Character The exact name in \code{df} from which to extract the data.}

\item{fps}{Numeric. Frame processing rate frequency profile (frames per second) of the dataset.}

\item{order}{Numeric. Order for the wavelet transformation. Default is \code{6}.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
If \code{return = TRUE}, returns a list with each Identifiers wavelet result. If provided, list is saved as rds to \code{rs.path}.
}
\description{
Uses the \code{\link{biwavelet::wt}} function to compute the wavelet transformation
for a given column.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
