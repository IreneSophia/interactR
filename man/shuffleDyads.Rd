% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{shuffleDyads}
\alias{shuffleDyads}
\title{Shuffle dyads to create pseudo-dyads}
\usage{
shuffleDyads(df, seed = NULL, considerTime = T, inOrder = F, nsim = 100)
}
\arguments{
\item{df}{Dataframe. Must contain the columns \code{Dyad}, \code{Time} and \code{Identifier}
to unequivocally identify one session. Each Dyad has one row per Identifier.
Dyad ID must consist of \verb{[Identifier]-[Identifier]}.}

\item{seed}{Numeric or other. Seed for reproducibility. If seed is not
numeric, then a random seed is chosen. Default is \code{NULL}.}

\item{considerTime}{Logical. Flags whether to exclude pseudo dyads with same
Time value. Default is \code{TRUE} corresponding to the exclusion.}

\item{inOrder}{Logical. Flags whether to keep assignment of Identifiers. Default
is \code{FALSE}, allowing left Identifiers to be assigned to right Identifier
in the pseudo-dyads.}

\item{nsim}{Numeric. Number of pseudo-dyads to be returned. Default is \code{100}.}
}
\value{
Returns a dataframe with pseudo-dyads, including the information of
\code{Dyad}, now consisting of both original dyad IDs, \code{Identifier}, \code{Time} as well as
\code{pseudoDyad}, a unique numeric ID, and \code{side}, tracking left versus right.
}
\description{
Takes a dataframe of dyads and returns a specific number of pseudo-dyads of
interaction partners who have not interacted with each other. Identifiers are
not paired with themselves, existing dyads are not re-paired, regardless of time,
and, if necessary, identifiers from the same time are not paired with each other.
Assignment (left versus right identifier in dyad ID) can be kept or ignored to
allow for the inclusion of conditions.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
