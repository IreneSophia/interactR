% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featWLCC.R
\name{extractWLCC}
\alias{extractWLCC}
\title{Extract Observed or Pseudo Windowed-Lagged Cross-Correlation (WLCC)}
\usage{
extractWLCC(
  df,
  winSample,
  incSample,
  lagSample,
  colname,
  rs.path = c(),
  suffix = "",
  pseudoSegment = F,
  nSegment = 100,
  df.pseudo = data.frame(),
  seed = NA,
  verbose = T,
  recompute = F,
  return = T
)
}
\arguments{
\item{df}{Dataframe. Must contain the columns \code{Dyad}, \code{Time}, \code{Identifier},
\code{Frame} as well as the column described by \code{colname}.}

\item{winSample}{Numeric. Window size in samples.}

\item{incSample}{Numeric. Window increment step in samples.}

\item{lagSample}{Numeric. Evaluated cross-correlation lag step in samples.}

\item{colname}{Character. Name of the column from which WLCC is extracted.}

\item{rs.path}{Character. Path to destination directory for saved files.
If empty (is.null(rs.path) == TRUE), then nothing is saved. Default is \code{c()}.}

\item{suffix}{Character. Suffix to be added to the files saved to disk. Default is \code{""}.}

\item{pseudoSegment}{Boolean. Switch to create pseudo WLCC instead by shuffling
the windows of the right Identifier before computing WLCC. Default is \code{FALSE}.}

\item{nSegment}{Numeric. Number of shuffling to be performed if pseudoSegment is TRUE.
Default is \code{100}.}

\item{df.pseudo}{Dataframe. If it contains rows, then instead of using the observed
dyads listed in \code{df}, these pairings are tested for pseudo-WTC. Dataframe can be
created using \code{\link[=shuffleIdentifier]{shuffleIdentifier()}} or \code{\link[=shuffleDyads]{shuffleDyads()}}. Default is \code{data.frame()}.}

\item{seed}{Numeric. Seed supporting reproducibility. Takes an integer seed or \code{NA} for random. Default is \code{NA}.
Only used, if pseudoSegment is TRUE, otherwise there is no random process executed in this function.}

\item{verbose}{Logical. Whether progress and output are printed to the console. Default is \code{TRUE}.}

\item{recompute}{Logical. Whether existing data on disk should be recomputed and overwritten. Default is \code{FALSE}.}

\item{return}{Logical. Whether the processed dataframe should be returned by the function. Default is \code{TRUE}.}
}
\value{
WLCC dataframe.
}
\description{
!ADD DETAILS!
}
\details{
If pseudo WLCC is computed, this should be either through setting pseudoSegment to TRUE
or by using a df.pseudo to provide shuffled dyads, but not both.
}
\author{
Irene Sophia Plank (\email{10planki@gmail.com})
}
